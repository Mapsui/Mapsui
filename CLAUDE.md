# Mapsui agent instructions

This is a compatibility entrypoint for Claude Code.

@AGENTS.md

Claude Code does not currently discover repository skills from `.agents/skills` automatically. When `AGENTS.md` selects a skill, read its `SKILL.md` from that directory explicitly.
