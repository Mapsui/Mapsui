# Set default behaviour, in case users don't have core.autocrlf set.
* text=auto
# Explicitly declare text files we want to always be normalized and converted 
# to native line endings on checkout.
*.cs text
# Custom for Visual Studio
*.sln    merge=union
*.csproj merge=union
*.vbproj merge=union
*.fsproj merge=union
*.dbproj merge=union
# Denote all files that are truly binary and should not be modified.
*.jpg binary
*.jpeg binary
# Git Lfs tracked files for regression Testing
Tests\Mapsui.Rendering.Skia.Tests\**\*.png filter=lfs diff=lfs merge=lfs -text
Tests\Mapsui.Rendering.Skia.Tests\**\*.sqlite filter=lfs diff=lfs merge=lfs -text
